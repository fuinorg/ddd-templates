package tst;

/**
 * Contains example classes that are splitted into an abstract and a concrete
 * (generate once) part. The classes are used in unit tests to compare them with
 * the actual result.
 */
